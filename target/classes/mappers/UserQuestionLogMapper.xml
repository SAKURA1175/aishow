<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxzd.study.mapper.UserQuestionLogMapper">

    <resultMap id="BaseResultMap" type="com.xxzd.study.domain.UserQuestionLog">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="question" column="question"/>
        <result property="topic" column="topic"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, question, topic, create_time
    </sql>

    <insert id="insert" parameterType="com.xxzd.study.domain.UserQuestionLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_question_log (user_id, question, topic, create_time)
        VALUES (#{userId}, #{question}, #{topic}, NOW())
    </insert>

    <select id="selectRecentByUser" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_question_log
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>
</mapper>
